#include <stdio.h>
#include <string.h>

int main()
{
    int n,i=0;
    scanf("%d", &n);
    char palavrasIngles[n][n], traducao[n][n], setas[n], palavras_por_frase[50], fraseTraduzida[1];
    for(int i = 0; i<n; i++)
    {
        scanf("%s %s %s%*c", palavrasIngles, setas, traducao);
    }
    while(scanf("%s%*c", frases[i]) != '*')
    {
        
    }
}