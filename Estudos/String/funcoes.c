strlen() -- retorna o tamanho da string. EX: tamanho = strlen(texto) -- tamanho = 3  // texto = ['abc']
strcpy() -- copia uma string para a outra. EX: (texto2,texto) -- texto2 = texto // texto2 = ['abc']
sltcat() -- realiza a juncao de duas strings. Ex: sltcat(texto,texto2) = ['abcabc']
strcmp() -- compara duas strings. ex: igual = strcmp(texto,texto2) -- igual = true //['abc'] == ['abc']
strncmp() -- compara n primeiros n digitos de duas strings. EX igual = strncmp(texto, texto2, 3) //retorna true